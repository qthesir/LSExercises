let phrase = "Always bet on JavaScript";

function quote(phrase) {
  console.log(phrase);
}

quote(phrase);

// The return value of the function is undefined because there is nothing explicitly
// returned, and that's JavaScripts default behavior with a function.
