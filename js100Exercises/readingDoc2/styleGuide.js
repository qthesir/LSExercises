// In the following code snippet, find all violations of the style guide. Rewrite it so that
// it conforms with the guide.

let iceCreamTaste = "chocolate";
let iceCreamDensity = 10;

while (iceCreamDensity > 0) {
  console.log("Drip...");
  iceCreamDensity--;
}

console.log(`The ${iceCreamTaste} ice cream melted.`);
