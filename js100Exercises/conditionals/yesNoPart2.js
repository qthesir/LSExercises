// Take your code from the previous exercise and rewrite the conditional so that it
// uses the ternary if-then-else operator.

let randomNumber = Math.round(Math.random());

num = randomNumber ? "Yes!" : "No.";

console.log(num);
